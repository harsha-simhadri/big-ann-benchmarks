Dec 5:
* We re-ran both private and public queeries using the latest ipmicap which captures nvidia board power.
* * 
* Note that due to crashes on the private query, we allowed config changes on the private eval.
* We used that same config on the re-eval of the public query set.

